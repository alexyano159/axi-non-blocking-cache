# License files -- personal, machine-locked, must never be committed.
*.dat
*.lic

# Questa/ModelSim simulation artifacts -- regenerated by every compile/run,
# specific to whoever's machine ran them, and can be large (work/ especially).
work/
transcript
vsim.wlf
*.wlf
modelsim.ini
*.mpf
*.mpf.bak

# Personal scratch notes (e.g. interview prep) -- kept local only, never
# pushed to the public repo. Module design docs live in docs/ instead.
private_notes/

# MATLAB step-through prototype -- kept for personal inspection only;
# the static mshr_dataflow_diagram.html covers the same ground and is
# the one actually meant for public/reviewer consumption.
visual_flow/mshr_simulator.m
